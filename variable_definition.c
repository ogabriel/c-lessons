#include <stdio.h>

int main() {
    // tells the compiler where and how much storage to create for the variable
    int foo;
    // the definition specifies the data type and contains a list of one or more variables of that type
    int bar, batata;
    // definition and initilization
    char barz = 'a';

    return 0;
}