#include <stdio.h>

int main()
{
    // format specifier for unsigned long
    printf("int: %lu \n", sizeof(int));
    printf("float: %lu \n", sizeof(float));
    printf("double: %lu \n", sizeof(double));
    printf("chat: %lu \n", sizeof(char));
    return 0;
}
